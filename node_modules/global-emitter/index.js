// Global Emitter
var Emitter = require('events');
var oEmitter = new Emitter();

module.exports = {
	emit: function() {
		oEmitter.emit(...arguments);
	},

	on: function() {
		oEmitter.on(...arguments);
	}
};
